import React from "react";

const Loading = () => (
   <div className="container loader-wrapper mx-auto flex justify-center p-8">
      <div className="loader"></div>
   </div>
);

export default Loading;
